import { PhysicalGoldTransactions } from './physical-gold-transactions';

describe('PhysicalGoldTransactions', () => {
  it('should create an instance', () => {
    expect(new PhysicalGoldTransactions()).toBeTruthy();
  });
});
